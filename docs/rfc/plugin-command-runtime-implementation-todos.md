# X-Mars 插件 Command Runtime 产品化实施方案

> 阶段：PCR（Plugin Command Runtime Productization）  
> 设计来源：[`docs/rfc/plugin-architecture-implementation-todos.md`](./plugin-architecture-implementation-todos.md) 的 PLUGIN-16 到 PLUGIN-21 剩余风险  
> 关联背景：[`docs/rfc/plugin-architecture-audit.md`](./plugin-architecture-audit.md)、[`docs/rfc/x-mars-vs-claude-code-min-granularity.md`](./x-mars-vs-claude-code-min-granularity.md)
> 更新时间：2026-05-02

## 阶段摘要

上一阶段已经把插件 command 从“manifest 声明”推进到“CLI 交互式 prompt 型 slash command”：支持 command Markdown 正文、positional 参数 schema、required/type/choices/default 校验、显式确认闸、额外参数拒绝和 quote-aware 参数解析。

本阶段继续把插件 command 产品化到 runtime 层。目标不是直接兼容任意 Claude Code command runtime，也不是让插件绕过 X-Mars 的 trust/permission/hook 体系，而是把当前 prompt-only command 的参数、执行、权限和诊断边界做成稳定宿主协议：

1. 参数解析从 positional-only 升级为稳定 command invocation object。
2. 命名 flag、repeatable 参数和 typed coercion 在进入模型或模块前完成。
3. prompt command 与 module command 共享同一套解析、权限、诊断和测试入口。
4. module command 必须经过 trust gate、安全 module resolver 和 command permission guard。
5. Claude Code 兼容仍采用静态导入/适配层，只映射可证明安全的参数与正文语义。

## 状态

- 阶段状态：Done
- 总完成百分比：100%
- 最新测试状态：Pass
- 最新测试命令：`pnpm prepublish:check`
- 最新测试结果：2026-05-02 23:57 Asia/Shanghai，全仓 lint、format、typecheck、test、build 均通过。此前 PCR-08 目标验证：`pnpm vitest run packages/tools/tests/claude-code-compat.test.ts packages/tools/tests/plugin-command-invocation.test.ts --reporter=verbose` 通过（2 个文件 16 项），tools typecheck/build 通过。
- 依赖项：`@x-mars/tools` plugin manifest / command registry / manager、`@x-mars/coding` InteractiveMode / AgentSession、`@x-mars/hooks` permission guard、`@x-mars/cli` interactive entry、Claude Code compat importer。
- 执行策略：先抽纯解析与 invocation contract，再接交互式 prompt command，最后引入受 trust/permission 管控的 module command runtime。

## 设计与验收链接

- 上一阶段账本：[`docs/rfc/plugin-architecture-implementation-todos.md`](./plugin-architecture-implementation-todos.md)
- 插件体系审计：[`docs/rfc/plugin-architecture-audit.md`](./plugin-architecture-audit.md)
- Claude Code 对比：[`docs/rfc/x-mars-vs-claude-code-min-granularity.md`](./x-mars-vs-claude-code-min-granularity.md)
- Command manifest：`packages/tools/src/plugin-manifest.ts`
- Command registry：`packages/tools/src/plugin-command-registry.ts`
- Claude Code importer：`packages/tools/src/claude-code-compat.ts`
- Interactive runtime：`packages/coding/src/modes/run-modes.ts`
- CLI interactive entry：`packages/cli/src/cli.ts`
- 现有测试：`packages/tools/tests/plugin-manifest.test.ts`、`packages/tools/tests/claude-code-compat.test.ts`、`packages/coding/tests/run-modes.test.ts`、`packages/cli/tests/cli.test.ts`

## 启动检查清单

- [x] 已读取上一阶段 PLUGIN-ARCH TODO 与剩余风险。
- [x] 已读取当前 `PluginCommandRegistry` 和 `InteractiveMode` 实现。
- [x] 已确认上一阶段工作区已提交：`5615ab1 feat: productize plugin command arguments`。
- [x] 已确认本阶段不回写 PLUGIN-ARCH 的完成百分比。
- [x] 开始实现前，重新读取目标模块当前代码。
- [x] 每个 TODO 开工前补充输入/输出接口、测试路径和验证方式。
- [x] 每个 TODO 完成前记录测试命令、测试结果和剩余风险。
- [x] 修改总完成百分比前，先用进度计算命令复核。

## 参考依据与需求方案设计备注

| 需求簇                 | 要解决的问题                                                                              | 期望结果                                                                                                                                   | 约束与边界                                                                                           | 方案选择                                                                                                           | 放弃方案                                                          | 验证方式                                                                               |
| ---------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Invocation object      | 当前运行链直接传 `string[]`，位置参数、flag、默认值和原始输入没有统一事实来源。           | 插件 command 执行前产出稳定 `PluginCommandInvocation`，包含 raw tokens、positional、named flags、coerced args、defaults、schema warnings。 | 不破坏无 schema command 的 freeform `$ARGUMENTS` 行为；普通非 slash prompt 不受影响。                | 在 `@x-mars/tools` 或 `@x-mars/coding` 抽纯解析模块，InteractiveMode 只消费解析结果。                              | 在 InteractiveMode 内继续堆校验函数，后续 module runtime 难复用。 | parser 单测、prompt command 回归、legacy freeform 测试。                               |
| 命名 flag              | 当前 `--flag value` 会被当作位置参数，无法表达 Claude Code 风格 command options。         | 支持 `--name value`、`--name=value`、boolean flag、短横线值、quoted value，并能进入 schema 校验。                                          | `--confirm-plugin` 是宿主确认 flag，不进入插件 schema；未知 flag 是否拒绝取决于 schema 是否声明。    | manifest argument 增加 `kind: positional/flag` 或 `flag` 字段，解析层先剥离宿主 flag，再匹配插件 flag。            | 直接把所有 `--x` 透传给模型，无法做权限和类型校验。               | unknown flag、missing flag value、boolean flag、confirm flag 隔离测试。                |
| Repeatable 参数        | 多文件、多标签、多工具选择无法用当前单值位置参数表达。                                    | schema 支持 repeatable 参数，运行时输出数组值，并在 prompt schema 中明确展示。                                                             | 默认值、choices、type coercion 必须对每个元素生效；repeatable positional 只能放在尾部或明确拒绝。    | `repeatable: true` 生成数组；flag repeatable 支持多次出现；positional repeatable 采用尾部吸收。                    | 允许任意位置 repeatable positional，解析歧义大。                  | 多次 flag、尾部文件列表、choices 数组校验测试。                                        |
| Typed coercion         | 当前 type 校验后仍把字符串送入 prompt/module，模块 runtime 还需要重复解析。               | 运行时产生 typed values：string、number、boolean、string array、number array、boolean array。                                              | Prompt command 仍需要可读字符串渲染；module command 使用 typed object。                              | 解析结果同时保留 `raw` 和 `value`，prompt renderer 使用 raw/format，module handler 使用 value。                    | 只在模块侧转换，prompt 和 module 行为容易分裂。                   | number/boolean coercion、非法值拒绝、prompt 渲染稳定测试。                             |
| Module command runtime | manifest 已有 `commands[].module` 风险来源，但当前不执行 command module。                 | 可信插件 command 可声明 module/exportName，宿主加载受控 handler，并通过 invocation object 调用。                                           | 必须经过 trust gate、安全 realpath resolver、loaded/unloaded lifecycle；handler 不直接拿裸 session。 | 定义 `PluginCommandHandler` 最小接口，输入为 invocation 与受限 host context，输出 system/response/prompt handoff。 | 直接 import 并传完整 App/session，权限面过大。                    | trusted/untrusted module command、resolver escape、handler success/error/unload 测试。 |
| Permission guard       | 现有 `--confirm-plugin` 是命令级确认，不等价于按 command capability 授权。                | command 可声明所需 permission，执行前产生 permission decision/audit；拒绝时不调用 prompt 或 module。                                       | 不替代工具级 permission hook；prompt command 也可要求 shell/network/filesystem 等能力确认。          | 接入现有 permission guard 或抽 command permission adapter；先覆盖 CLI interactive 路径。                           | 只依赖 trust，一旦 trusted 就无限制执行 command。                 | permission allow/deny/audit 测试、未授权不调用 handler 测试。                          |
| Diagnostics            | 用户无法区分 parse failed、permission denied、handler failed、prompt command 已交给模型。 | CLI/system message 与 diagnostics 暴露结构化状态，便于审计和插件调试。                                                                     | 不泄露 prompt 正文或 secret；错误信息包含 pluginId/command/stage。                                   | 为 invocation、permission、runtime result 定义诊断事件，CLI 先展示文本摘要。                                       | 只返回普通字符串，后续 Devtools/日志无法消费。                    | error snapshot、diagnostics redaction、CLI message 测试。                              |
| Claude Code compat     | Claude Code command 参数语义可能继续演进，X-Mars 不能直接假设运行时兼容。                 | importer 尽量映射 argument frontmatter、argument-hint、正文和静态 options；无法映射的字段进入 migration warning。                          | 不直接运行 Claude Code runtime；不导入未确认安全的 hooks/bin/settings 行为。                         | 扩展 compat importer 的参数映射和 warning report，保持 X-Mars manifest 是唯一执行事实。                            | 静默丢弃 unsupported 字段，用户误以为完全兼容。                   | fixture 转换、unsupported warning、导入后执行路径测试。                                |

## TODO 表

| ID     | 状态 | 完成百分比 | 测试通过 | 文件/模块                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 功能介绍                                                                                                                | 完成时设计核对                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------ | ---- | ---------: | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PCR-01 | Done |       100% | Pass     | `packages/tools/src/plugin-command-invocation.ts`、`packages/tools/tests/plugin-command-invocation.test.ts`、`packages/coding/src/modes/run-modes.ts`、`packages/tools/src/index.ts`                                                                                                                                                                                                                                                                                                                                                                                     | 抽出插件 command invocation 解析对象，统一 raw tokens、positional 参数、宿主 flag、默认值、校验错误和 prompt 渲染输入。 | 已实现输入/输出：新增 `PluginCommandInvocation` 与 `buildPluginCommandInvocation()`，输入为 command manifest、已解析 positional tokens 和确认状态，输出为成功 invocation 或结构化 missing/unexpected/invalid error；保留 legacy freeform command 任意参数行为；默认值、required、type、choices、额外参数拒绝统一迁入 `@x-mars/tools`，`InteractiveMode` 只消费 invocation result。`--confirm-plugin` 继续由宿主 flag 剥离，不进入插件位置参数。测试覆盖 defaults/confirmation metadata、结构化错误、legacy freeform、schema overflow、confirmation flag 隔离，以及既有交互式 slash command 回归。验证：`pnpm vitest run packages/tools/tests/plugin-command-invocation.test.ts packages/coding/tests/run-modes.test.ts packages/cli/tests/cli.test.ts --reporter=verbose` 通过（3 个文件 44 项）；`pnpm --filter @x-mars/tools typecheck`、`pnpm --filter @x-mars/tools build`、`pnpm --filter @x-mars/coding typecheck`、`pnpm --filter @x-mars/coding build`、`pnpm --filter @x-mars/cli typecheck` 通过。剩余风险：PCR-01 仍只处理 positional invocation；命名 flag、repeatable、typed coercion object 和 module runtime 留给 PCR-02 到 PCR-05。                                                      |
| PCR-02 | Done |       100% | Pass     | `packages/tools/src/plugin-manifest.ts`、`packages/tools/src/claude-code-compat.ts`、`packages/tools/src/plugin-command-invocation.ts`、`packages/tools/tests/plugin-manifest.test.ts`、`packages/tools/tests/claude-code-compat.test.ts`、`packages/tools/tests/plugin-command-invocation.test.ts`、`packages/coding/tests/run-modes.test.ts`                                                                                                                                                                                                                           | 为 command 参数 schema 增加命名 flag 声明，并从 Claude Code 兼容导入器映射可识别 options。                              | 已实现输入/输出：`PluginCommandArgumentManifest` 新增 `flag` / `alias` 字段，manifest 校验会拒绝前导横线、宿主保留 `confirm-plugin` 和重复 flag/alias；Claude Code compat importer 会导入 frontmatter 中的 `arguments[].flag` 与 `arguments[].alias`；`buildPluginCommandInvocation()` 在 defaults/required/type/choices 校验前解析 `--name value`、`--name=value` 和 boolean flag，无匹配 flag 且 command 声明了 flag 时返回 unexpected error；`PluginCommandInvocation` 新增 `namedArguments`，同时保留 `arguments` 作为 prompt command 的顺序输入。InteractiveMode 回归覆盖 `/review --path "src/app shell.ts" --dry-run`。验证：目标 vitest 4 个文件 53 项通过；`pnpm exec tsc -p packages/tools/tsconfig.json --noEmit` 通过；`pnpm exec tsdown --config packages/tools/tsdown.config.ts` 通过。剩余风险：短 alias 仍采用长 flag 名形式，不支持单横线 `-p`；repeatable 参数、typed coercion object 和 module command runtime 留给 PCR-03 到 PCR-05。                                                                                                                                                                                                                                                |
| PCR-03 | Done |       100% | Pass     | `packages/tools/src/plugin-manifest.ts`、`packages/tools/src/claude-code-compat.ts`、`packages/tools/src/plugin-command-invocation.ts`、`packages/tools/tests/plugin-manifest.test.ts`、`packages/tools/tests/claude-code-compat.test.ts`、`packages/tools/tests/plugin-command-invocation.test.ts`、`packages/coding/tests/run-modes.test.ts`                                                                                                                                                                                                                           | 支持 repeatable 参数，覆盖多文件、多标签等数组型 command 输入。                                                         | 已实现输入/输出：`PluginCommandArgumentManifest` 新增 `repeatable?: boolean`，manifest 校验会拒绝非 boolean repeatable，并拒绝非尾部 repeatable positional，避免解析歧义；Claude Code compat importer 会导入 frontmatter 中的 `arguments[].repeatable`；`buildPluginCommandInvocation()` 现在以解析期 `valuesByName` 作为 defaults/required/type/choices 的事实来源，repeatable flag 支持多次出现，repeatable positional 采用尾部吸收，`PluginCommandInvocation.repeatedArguments` 输出数组值，`arguments` 仍保留给 prompt 渲染的扁平字符串序列。解析层改为显式记录 schema overflow token，避免 default 或 repeatable 展示值被误判为 unexpected。测试覆盖 repeatable flag、repeatable positional tail、repeatable choices 每项校验、非尾部 repeatable schema 拒绝，以及 PCR-01/PCR-02 回归。验证：目标 vitest 4 个文件 56 项通过；`pnpm exec tsc -p packages/tools/tsconfig.json --noEmit` 通过；`pnpm exec tsdown --config packages/tools/tsdown.config.ts` 通过；`pnpm format` 通过。剩余风险：当前仍只输出字符串数组事实，typed coercion object 留给 PCR-04。                                                                                                                                         |
| PCR-04 | Done |       100% | Pass     | `packages/tools/src/plugin-command-invocation.ts`、`packages/tools/src/index.ts`、`packages/tools/tests/plugin-command-invocation.test.ts`、`packages/coding/src/modes/run-modes.ts`、`packages/coding/tests/run-modes.test.ts`                                                                                                                                                                                                                                                                                                                                          | 实现 typed coercion object，prompt command 和 module command 共享同一解析事实。                                         | 已实现输入/输出：`PluginCommandInvocation` 新增 `typedArguments`，值类型为 string/number/boolean/array，由同一份解析期 `valuesByName` 和 manifest type/repeatable/default 规则生成；非 repeatable 参数输出标量，repeatable 参数输出数组；`arguments` 继续保留 prompt command 的扁平字符串展示输入，`InteractiveMode` 仍只把 display arguments 传给 `renderPluginCommandPrompt()`，避免把模块用 typed object 泄露到 prompt 正文。测试覆盖 number、boolean、repeatable number array、默认 boolean、flag 输入和 prompt display 不变。验证：目标 vitest 4 个文件 57 项通过；`pnpm exec tsc -p packages/tools/tsconfig.json --noEmit` 通过；`pnpm exec tsdown --config packages/tools/tsdown.config.ts` 通过；`pnpm format` 通过。剩余风险：typed object 目前尚未被 module handler 消费，handler 接口和 trust gate 留给 PCR-05。                                                                                                                                                                                                                                                                                                                                                                              |
| PCR-05 | Done |       100% | Pass     | `packages/tools/src/plugin-command-handler.ts`、`packages/tools/src/plugin-manifest.ts`、`packages/tools/src/plugin-manager.ts`、`packages/tools/src/plugin-command-registry.ts`、`packages/coding/src/app/x-mars-app.ts`、`packages/coding/src/modes/run-modes.ts`、`packages/tools/tests/plugin-manager.test.ts`、`packages/coding/tests/run-modes.test.ts`                                                                                                                                                                                                            | 定义并加载受 trust gate 管控的 plugin command module handler。                                                          | 已实现输入/输出：新增 `PluginCommandHandler` 协议，handler 输入为 `PluginCommandInvocation` 与只包含 `pluginId`/`command` 的受限 context，输出为 `system`、`response` 或 `prompt` handoff；`PluginManager` 只在 command adapter 存在且插件已通过 trust gate 后，通过现有 realpath resolver 导入 `commands[].module` / `exportName`；`PluginCommandRegistry` 保存可选 handler，`XMarsApp` adapter 将 handler 注册进 registry；`InteractiveMode` 在 parser/confirmation 校验后优先执行 handler，`prompt` handoff 复用当前 session，handler throw 转换为 system error。unload 仍走既有 `unregisterCommand`，从 registry 移除 handler。测试覆盖 trusted handler 加载、untrusted 不 import/无 side effect、resolver escape、registry handler 保存、typed invocation handler 执行和 prompt handoff。验证：目标 vitest 5 个文件 72 项通过；`pnpm exec tsc -p packages/tools/tsconfig.json --noEmit` 通过；`pnpm exec tsdown --config packages/tools/tsdown.config.ts` 通过；`pnpm format` 通过。剩余风险：command permission guard 与结构化 diagnostics 留给 PCR-06/PCR-07。                                                                                                                                    |
| PCR-06 | Done |       100% | Pass     | `packages/tools/src/plugin-manifest.ts`、`packages/tools/src/plugin-manager.ts`、`packages/tools/src/plugin-command-registry.ts`、`packages/coding/src/modes/run-modes.ts`、`packages/cli/src/cli.ts`、`packages/coding/tests/run-modes.test.ts`、`packages/tools/tests/plugin-manifest.test.ts`、`packages/tools/tests/plugin-manager.test.ts`、`packages/tools/tests/plugin-command-registry.test.ts`、`packages/hooks/tests/permission.test.ts`                                                                                                                       | 为插件 command 执行接入 permission guard 和审计证据。                                                                   | 已实现输入/输出：`PluginCommandManifest` 新增 `permissions?: PluginPermission[]` 并复用 manifest 既有 permission 校验；`PluginManager` 将 command permissions 纳入 trust gate，未 trusted 的危险 command permission 不注册、不触发 adapter；`PluginCommandRegistry` 克隆 command 时保留 permissions；`InteractiveMode` 在 prompt/model 或 module handler 执行前，用 `PermissionPolicyRegistry` 评估每个 command permission，并以 `plugin-command:<permission>` 作为 synthetic toolName 写入 `PermissionAuditLog`，metadata 包含 pluginId、commandName、permission、confirmed 和 sessionId；deny/ask 返回 system message 且不调用 handler 或模型，allow 继续进入 handler/prompt；CLI interactive entry 将 app 的 permissionRegistry/auditLog 传入运行时。测试覆盖 manifest command permission 校验、trust gate skip、registry clone、deny 阻断 handler、allow audit 和既有 permission guard 回归。验证：目标 vitest 5 个文件 135 项通过；`pnpm exec tsc -p packages/tools/tsconfig.json --noEmit` 通过；`pnpm exec tsdown --config packages/tools/tsdown.config.ts` 通过；`pnpm format` 通过。剩余风险：当前只输出文本型 permission decision，结构化 diagnostics、redaction 和 Devtools 消费留给 PCR-07。 |
| PCR-07 | Done |       100% | Pass     | `packages/agent/src/session-events.ts`、`packages/agent/src/index.ts`、`packages/coding/src/session/types.ts`、`packages/coding/src/session/agent-session.ts`、`packages/coding/src/modes/run-modes.ts`、`packages/devtools/src/audit-trace.ts`、`packages/service/src/session-event-router.ts`、`packages/protocol/src/types.ts`、`packages/protocol/src/validation.ts`、`packages/coding/tests/run-modes.test.ts`、`packages/devtools/tests/audit-trace.test.ts`、`packages/service/tests/session-event-router.test.ts`、`packages/protocol/tests/ws-protocol.test.ts` | 补齐 command parse、permission、runtime result 的诊断事件和 CLI/system message。                                        | 已实现输入/输出：新增 `PluginCommandDiagnostic` session event，字段包含 pluginId、commandName、stage、status、confirmed、permission/effect/reason、resultType、rawArgumentCount、argumentNames、typedArgumentKeys，不记录完整 prompt 正文或参数值；`InteractiveMode` 在 parse failed/completed、permission allow/deny/ask、handler started/completed/failed/handoff、prompt handoff/completed 阶段记录诊断；`AgentSession.recordPluginCommandDiagnostic()` 同时发布 session event 并写入 Devtools `plugin.command` audit trace；`AuditTraceRecorder` 支持 `plugin.command` 类型和 redaction；service router 将事件映射为 `Plugin.commandDiagnostic` WebSocket 消息；protocol 类型与 validator 接受该消息。CLI/system message 继续沿用 PCR-06 的用户可读文本，结构化诊断供 Devtools/服务端消费。测试覆盖 parse/handler 诊断、不泄露 prompt、audit trace redaction/replay、session event 到 WS 路由、protocol validation。验证：目标 vitest 4 个文件 47 项通过；agent/devtools/protocol/service/coding typecheck 通过；agent/protocol/devtools/service/coding build 通过；format 通过。剩余风险：Claude Code compat unsupported runtime 字段矩阵与 importer warning 留给 PCR-08。                          |
| PCR-08 | Done |       100% | Pass     | `packages/tools/src/claude-code-compat.ts`、`packages/tools/tests/claude-code-compat.test.ts`、`docs/rfc/plugin-command-runtime-implementation-todos.md`                                                                                                                                                                                                                                                                                                                                                                                                                 | 扩展 Claude Code command 兼容矩阵，明确已映射、部分映射和 unsupported runtime 字段。                                    | 已实现输入/输出：Claude Code command importer 继续只映射 Markdown body、name、description、`arguments[]` 和 `argument-hint` 到 X-Mars manifest；新增 command frontmatter unsupported 扫描，`hooks`、`bin`、`settings`、`runtime`、`permissions`、`allowed-tools`、`model`、`output-style` 以及未知字段都会写入 `report.unsupported`，并输出 migration warning；manifest 顶层 `runtime`、`settings`、`bin` 也记录 unsupported。文档新增 Claude Code Command Compat Matrix，明确 Mapped/Partial/Unsupported 状态，并记录 X-Mars 不直接运行 Claude Code command runtime、不把 unsupported 字段转为 module handler。测试覆盖 unsupported runtime fields report/warning、导入后 command 不带 `module`，以及既有 argument schema/invocation 回归。验证：目标 vitest 2 个文件 16 项通过；tools typecheck/build 通过；format 通过。                                                                                                                                                                                                                                                                                                                                                                              |

## Claude Code Command Compat Matrix

| Claude Code command 字段/来源       | X-Mars 映射状态 | X-Mars 处理方式                                                           | 运行时边界                                                                                    |
| ----------------------------------- | --------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| Markdown body                       | Mapped          | 导入为 `PluginCommandManifest.prompt`                                     | 仅作为 prompt command 正文；不执行 Claude Code runtime。                                      |
| `name` / 文件名                     | Mapped          | 导入为 command `name`                                                     | X-Mars slash command 名称由 manifest 校验。                                                   |
| `description`                       | Mapped          | 导入为 command `description`                                              | 仅用于 help/prompt 描述。                                                                     |
| `arguments[]`                       | Mapped          | 导入 name/description/required/type/flag/alias/repeatable/choices/default | 进入 X-Mars `PluginCommandInvocation`，由 X-Mars parser/type coercion/permission guard 执行。 |
| `argument-hint`                     | Partial         | 解析 `<required>` / `[optional]` 为 positional schema                     | 不推断 type、choices、flag 或 runtime 行为。                                                  |
| `hooks` / `bin` / `settings`        | Unsupported     | 写入 `report.unsupported`，并输出 migration warning                       | 不导入、不执行、不转为 X-Mars module handler。                                                |
| `runtime` / `permissions` / `model` | Unsupported     | 写入 `report.unsupported`，并输出 migration warning                       | X-Mars 只信任自身 manifest permissions/trust/runtime，不继承 Claude Code runtime 策略。       |
| 其他未知 frontmatter                | Unsupported     | 写入 `report.unsupported`，并输出 migration warning                       | 需要后续显式设计后才能映射。                                                                  |

## 阶段完成标准

- `PluginCommandInvocation` 成为 prompt command 与 module command 的唯一执行输入事实来源。
- 命名 flag、repeatable 参数、typed coercion、default、choices、required、unexpected input 均有单元测试和交互式回归测试。
- module command 只有 trusted 插件可加载，且路径 resolver、unload lifecycle 和 handler error 均可诊断。
- command permission guard 在执行前生效，拒绝路径不调用模型、不调用 handler。
- Claude Code importer 对可映射字段写入 X-Mars manifest，对不可映射字段输出 migration warning。
- 本阶段完成时必须执行目标测试、相关包 typecheck/build；跨模块行为稳定后执行 `pnpm prepublish:check`。

## 状态更新规则

- `状态` 只能使用 `Not Started`、`In Progress`、`Done`。
- `测试通过` 只能使用 `Pass`、`Fail`、`Not Run`。
- `完成百分比` 使用保守整数。
- 只有实现、测试和设计核对均满足时，单项才能标为 100%。
- 阶段总完成百分比按 TODO 行百分比算术平均并四舍五入计算；当前为 8 项，8 项均 100%，总完成百分比为 100%。
- 修改任一 TODO 百分比前，必须先读取对应实现和测试，不得只基于计划调整。

进度计算命令：

```sh
TODO_FILE=docs/rfc/plugin-command-runtime-implementation-todos.md
awk -F'|' '
/^\| PCR-[0-9]+ / {
  gsub(/ /, "", $4);
  gsub(/%/, "", $4);
  sum += $4;
  count += 1
}
END {
  if (count == 0) {
    printf "count=0 sum=0 average=0.00 rounded=0\n"
  } else {
    printf "count=%d sum=%d average=%.2f rounded=%d\n", count, sum, sum / count, int(sum / count + 0.5)
  }
}' "$TODO_FILE"
```

## RFC-to-TODO 执行记录

- 2026-05-02 23:13 Asia/Shanghai：按 `rfc-to-todos/SKILL.md` 方法论新建 `PCR` 阶段实施账本。依据 PLUGIN-ARCH 的 PLUGIN-16 到 PLUGIN-21 剩余风险，拆出 8 个最小可验证 TODO，覆盖 invocation object、命名 flag、repeatable、typed coercion、module command runtime、permission guard、diagnostics 和 Claude Code compat matrix。本轮为方案与 TODO 生产，未修改 runtime 代码，测试未运行。
- 2026-05-02 23:18 Asia/Shanghai：完成 `PCR-01`。新增 `@x-mars/tools` 纯 `plugin-command-invocation` 模块，集中产出 `PluginCommandInvocation` 与 missing/unexpected/invalid 结构化错误；`InteractiveMode` 改为消费 invocation result，原测试辅助函数通过 `@x-mars/tools` re-export 保持兼容。验证：目标 vitest 3 个文件 44 项通过，tools/coding/cli typecheck 通过，tools/coding build 通过。剩余风险：命名 flag、repeatable、typed coercion object 和 module command runtime 仍待后续 TODO。
- 2026-05-02 23:19 Asia/Shanghai：继续推进全仓验证收口。`PCR-01` 变更完成后执行 `pnpm format` 和 `pnpm prepublish:check`；全仓 lint、format、typecheck、test、build 均通过，lint 为 0 warning / 0 error。
- 2026-05-02 23:24 Asia/Shanghai：完成 `PCR-02`。command 参数 schema 新增 `flag` / `alias`，compat importer 支持导入同名字段；invocation runtime 支持 `--flag value`、`--flag=value` 和 boolean flag，未知 flag 在声明了 flag 的 command 中会被拒绝，宿主 `--confirm-plugin` 仍在进入插件参数前剥离。验证：目标 vitest 4 个文件 53 项通过，tools typecheck/build 通过。全仓 typecheck 当前受工作区外部包名/类型迁移改动影响，未作为本轮完成证据。
- 2026-05-02 23:31 Asia/Shanghai：完成 `PCR-03`。command 参数 schema 新增 `repeatable`，manifest 校验约束 repeatable positional 必须位于尾部；invocation runtime 支持 repeatable flag 多次出现、repeatable positional 尾部吸收、每项 choices/type 校验，并新增 `repeatedArguments` 作为数组事实来源。验证：目标 vitest 4 个文件 56 项通过，tools typecheck/build 通过，format 通过。全仓 typecheck 当前受工作区外部包名/类型迁移改动影响，未作为本轮完成证据。
- 2026-05-02 23:34 Asia/Shanghai：完成 `PCR-04`。`PluginCommandInvocation` 新增 `typedArguments`，由同一解析事实源生成 string/number/boolean 标量和 repeatable array；prompt command 继续使用 `arguments` 字符串序列，module command 后续可直接消费 typed object。验证：目标 vitest 4 个文件 57 项通过，tools typecheck/build 通过，format 通过。全仓 typecheck 当前受工作区外部包名/类型迁移改动影响，未作为本轮完成证据。
- 2026-05-02 23:41 Asia/Shanghai：完成 `PCR-05`。新增 `PluginCommandHandler` 协议，trusted 插件 command module 在 `PluginManager` load 阶段通过安全 resolver 导入并注册到 `PluginCommandRegistry`，`InteractiveMode` 支持 handler 返回 system/response/prompt handoff；untrusted command module 不 import。验证：目标 vitest 5 个文件 72 项通过，tools typecheck/build 通过，format 通过。`packages/coding` typecheck 当前受工作区外部包名/类型迁移改动影响失败，未作为本轮完成证据。
- 2026-05-02 23:46 Asia/Shanghai：完成 `PCR-06`。command manifest 支持 `permissions` 声明并复用既有 permission 枚举校验；dangerous command permission 纳入插件 trust gate；`InteractiveMode` 在 prompt/model 或 module handler 前执行 command permission decision，deny/ask 阻断后续执行，allow 写入 audit evidence；CLI 将 app permission registry 和 audit log 接入 interactive runtime。验证：目标 vitest 5 个文件 135 项通过，tools typecheck/build 通过，format 通过。`packages/coding` typecheck 当前受工作区外部包名/类型迁移改动影响失败，未作为本轮完成证据。
- 2026-05-02 23:53 Asia/Shanghai：完成 `PCR-07`。新增插件 command 结构化诊断事件，覆盖 parse、permission、handler、prompt 阶段；`AgentSession` 将诊断发布为 session event 并写入 Devtools `plugin.command` audit trace；service router 映射为 `Plugin.commandDiagnostic` WebSocket 消息，protocol 类型与 validator 同步接受；诊断只记录阶段、状态、参数键名/数量和 redacted message，不记录完整 prompt 正文或参数值。验证：目标 vitest 4 个文件 47 项通过，agent/devtools/protocol/service/coding typecheck 通过，agent/protocol/devtools/service/coding build 通过，format 通过。
- 2026-05-02 23:56 Asia/Shanghai：完成 `PCR-08` 并收口 PCR 阶段。Claude Code command importer 新增 unsupported runtime frontmatter 扫描和 migration warning；hooks/bin/settings/runtime/permissions/allowed-tools/model/output-style 等字段只进入 report，不会启用 X-Mars module handler 或 Claude Code runtime 行为；文档新增 Mapped/Partial/Unsupported 兼容矩阵。验证：目标 vitest 2 个文件 16 项通过，tools typecheck/build 通过，format 通过。阶段总进度复算为 100%。
- 2026-05-02 23:57 Asia/Shanghai：执行阶段完成验证 `pnpm prepublish:check`。首次运行只因 TODO 文档格式化失败，执行 `pnpm format` 后重跑；第二次全仓 lint、format、typecheck、test、build 均通过。期间发现工作区已有 `x-mars-app.ts` -> `x-mars-app.ts` 重命名迁移残留，修正新增测试 `packages/coding/tests/x-mars-app-slot-routing.test.ts` 的 import 路径后全仓测试通过。
