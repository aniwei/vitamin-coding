# Claude-Code 借鉴落地 TODO 跟踪器

> 阶段：CCAG（Claude-Code Agent Framework Gap）
> 设计来源：[`docs/rfc/claude-code-source-analysis.md`](./claude-code-source-analysis.md)
> 来源仓库：`https://github.com/aniwei/Claude-Code.git`
> 更新时间：2026-05-02

## 阶段摘要

本阶段将 Claude Code 源码对比审计结论转为 Vitamin-Coding agent 框架的可执行改进账本。设计事实来源是 `claude-code-source-analysis.md` 中的“Vitamin-Coding 借鉴分析”，当前实现事实来源是 `packages/agent`、`packages/tools`、`packages/memory`、`packages/coding`、`packages/hooks`、`packages/service`、`packages/session`、`packages/ai` 的代码与测试。

## 状态

- 阶段状态：Done
- 总完成百分比：100%
- 最新测试状态：Pass
- 最新测试命令：`pnpm prepublish:check`
- 最新测试结果：2026-05-02 04:37 Asia/Shanghai，发布前总门禁通过，串联执行 `pnpm lint && pnpm typecheck && pnpm test && pnpm build` 成功；其中 `pnpm lint` 通过（oxlint 0 个 warning、0 个 error；oxfmt 全仓格式通过），`pnpm typecheck` 全仓 21 个项目通过，`pnpm test` 全仓 144 个测试文件中 142 个通过、2 个 skipped，1360 个测试用例中 1358 个通过、2 个 skipped，`pnpm build` 全仓 22 个项目通过。此前沙箱内失败已确认为 localhost 监听、`~/.vitamin` 写入和外网 DNS 环境限制，本轮未复现。
- 已知全仓类型检查缺口：无，2026-05-02 已修复 `packages/invariant`、`packages/setting`、`packages/swarm` 剩余 typecheck 失败。
- 依赖项：`@vitamin/agent` 工具执行状态机、`@vitamin/tools` 工具注册元数据、`@vitamin/memory` 压缩与记忆、`@vitamin/session` 会话持久化、`@vitamin/service` 事件桥接。
- 执行策略：先固定当前已实现能力和证据，再按风险优先补齐安全边界、上下文预算、工具执行、会话恢复和插件体系。

## 设计与验收链接

- RFC：[`docs/rfc/claude-code-source-analysis.md`](./claude-code-source-analysis.md)
- 工具执行：`packages/agent/src/tool-executor.ts`、`packages/agent/src/deferred-tools.ts`
- 工具注册：`packages/tools/src/registry.ts`、`packages/tools/src/register-builtin.ts`
- 权限系统：`packages/hooks/src/core/permission/*`、`packages/coding/tests/permission-integration.test.ts`
- 上下文与记忆：`packages/memory/src/*`、`packages/coding/src/hooks/auto-compaction.ts`、`packages/coding/src/hooks/memory-extraction.ts`
- 会话：`packages/session/src/types.ts`、`packages/session/src/in-memory-session.ts`
- Prompt Cache：`packages/prompt/src/prompt-cache.ts`、`packages/ai/tests/prompt-cache.test.ts`
- 远程事件：`packages/service/src/websocket-manager.ts`

## 启动检查清单

- [x] 已读取 Claude-Code 来源 RFC。
- [x] 已核对当前 agent、tool、memory、session、service 关键实现路径。
- [x] 已执行目标测试，确认当前增量测试通过。
- [x] 已按 RFC-to-TODO 方法论记录任务状态、证据和缺口。
- [x] 已在各 TODO 的完成时设计核对中补充输入输出边界、测试证据、实现路径和剩余风险说明。

## TODO 表

| ID      | 状态 | 完成百分比 | 测试通过 | 文件/模块                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 功能介绍                                                                                                                             | 完成时设计核对                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------- | ---- | ---------: | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CCAG-01 | Done |       100% | Pass     | `packages/hooks/src/core/permission/*`、`packages/coding/src/app/vitamin-app.ts`、`packages/tools/src/shell/*`、`packages/mcp/src/mcp-client.ts`、`packages/agent/src/tool-executor.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 权限引擎硬化：借鉴 Claude Code 的工具级权限路由、敏感操作确认、deny-first 合并与审计链路，补齐 shell、网络、敏感路径和 bypass 边界。 | 已新增 `builtin::non-bypassable-safety`，在 `bypass` 前硬拒绝受保护路径写入和根/家目录递归删除；修复空 `fileWriteTools` 被误当成全工具匹配的边界；新增 `filePaths` 多路径判定，覆盖 `oldPath/newPath/outputPath/targetPath` 等参数，并要求 directory-freeze 下所有路径都在允许目录内；配置型 policy priority 被限制到 builtin safety/boundary 之后，负优先级无法绕过不可绕过规则；新增 `builtin::network-safety`，在 permission 层审计并拒绝 web/MCP 网络工具访问 localhost、私网、metadata、file/data/javascript 等目标；MCP SSE server URL 在 connect 前拒绝 localhost、私网、metadata 和非法协议；`ask` 决策在 tool executor 中 fail-closed，缺少 approval handler 时拒绝执行，approve/reject 均有测试；`@vitamin/hooks`、`@vitamin/mcp`、`@vitamin/tools`、`@vitamin/agent` typecheck 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CCAG-02 | Done |       100% | Pass     | `packages/memory/src/*`、`packages/coding/src/hooks/auto-compaction.ts`、`packages/prompt/src/prompt-cache.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 上下文预算规划器：在模型调用前预测 token、缓存分界、压缩策略和状态恢复载荷，避免只在过长后被动修复。                                 | 已新增 `planContextBudget()` 与 `MemoryManager.planContextBudget()`，输出当前 token 估算、预留输出、可用输入、prune/micro/compact 阈值、剩余输入 token、利用率、推荐动作和 trace；已接入模型调用前 `messages.transform` 的 auto-compaction hook，低水位直接跳过处理，高水位才进入压缩管线；process 后记录 `before/after` 预算、策略列表、`tokensBefore/tokensAfter/tokensSaved` 和 changed 状态，形成压缩前后 token 账本；覆盖 none/prune/compact/snip 决策、hook skip/process 和账本测试。PTL 分组截断保留为后续长上下文错误恢复增强，不阻塞本项预算规划器完成。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| CCAG-03 | Done |       100% | Pass     | `packages/agent/src/tool-executor.ts`、`packages/agent/src/work-loop.ts`、`packages/agent/src/session-events.ts`、`packages/coding/src/session/agent-session.ts`、`packages/service/src/session-event-router.ts`、`packages/opendev-ui/src/stores/chat.ts`、`packages/agent/tests/tool-executor.test.ts`、`packages/agent/tests/agent-loop.test.ts`、`packages/service/tests/session-event-router.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                 | 流式工具执行：借鉴 StreamingToolExecutor，把工具执行事件、权限确认、进度、部分结果和最终结果作为结构化事件流输出。                   | 已新增 `ToolExecutionEvent` 与 `ToolExecutor.executeStream()`，和现有 `execute()` 共用同一条内部执行路径；事件覆盖 started、approval_required、approval_resolved、progress、error、result；工具 `onUpdate` 会转为 progress 事件；work-loop 已改为消费 `executeStream()` 并发出 `tool_execution_event`，同时保留旧 `tool_call_start/end`；session/service 已桥接为 `Chat.toolExecutionEvent`，前端先将 progress 附加到对应工具消息；正常执行、审批批准、审批拒绝、工具异常、progress 透传、旧 emitter 兼容和 service 路由均有测试；`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service` typecheck 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CCAG-04 | Done |       100% | Pass     | `packages/orchestrator/src/types.ts`、`packages/orchestrator/src/executor.ts`、`packages/orchestrator/src/orchestrator.ts`、`packages/orchestrator/src/task-store.ts`、`packages/tools/src/orchestration/task-delegate.ts`、`packages/coding/src/app/vitamin-app.ts`、`packages/coding/src/session/agent-session.ts`、`packages/coding/src/session/coding-session-manager.ts`、`packages/coding/src/session/hooks.ts`、`packages/hooks/src/core/permission/*`、`packages/agent/src/types.ts`、`packages/orchestrator/tests/executor.test.ts`、`packages/orchestrator/tests/task-store.test.ts`、`packages/orchestrator/tests/orchestrator.test.ts`、`packages/tools/tests/extended-tools.test.ts`、`packages/coding/tests/vitamin-app-slot-routing.test.ts`、`packages/hooks/tests/permission.test.ts`、`packages/swarm`、`packages/agent` | Sidechain Subagent 隔离：借鉴 Claude Code Task/AgentTool，把子 agent 对话、工具权限、工作区和摘要回传与主会话隔离。                  | 已在 orchestrator 层新增 sidechain 执行契约：`TaskInput.sidechain`、`SidechainPolicy`、`SidechainContext`、`RunSessionOptions.sidechain` 和 `RunSessionResult.summary/transcript`；委派任务执行时会向宿主 `runSession` 传入 taskId、parentTaskId、parentSessionId、subagent/category、returnMode、permissionMode、workspaceRoot、allowedTools/deniedTools；`task_delegate` 会从工具上下文透传当前 `sessionId` 为 `parentSessionId`，并支持 `timeoutMs` 透传到 sidechain policy；`VitaminApp` 的 `runSession` 已接入真实 sidechain 生命周期，支持从父 session fork sticky/ephemeral child session，运行后返回 summary/transcript，ephemeral child 在成功、失败、abort 和 timeout 路径都会进入 `finally` 清理，sticky child 可保留复用；`CodingSessionManager.forkSession()` 支持覆盖 child agentName、tools、workspaceDir 和 permissionMetadata；restricted sidechain 会按 allowedTools/deniedTools 过滤 child tools，workspaceRoot 会覆盖 child workspace；默认 `summary_only`，父任务返回与 `TaskOutput.text` 只使用 summary 或截断摘要，完整 child transcript 仅记录在 task 的 `sidechain.transcript` 元数据中，不暴露给 `task_get` 输出；sidechain 失败时 executor 会记录 childSessionId、失败摘要和可用 transcript 到 task sidechain metadata，避免父任务只剩裸错误；`RunSessionOptions.signal` 已接入 TaskExecutor active AbortController、Orchestrator cancelBackground/updateTask cancel 入口和 AgentSession prompt，运行中取消会 abort child agent，且 runSession 晚返回也不会把 cancelled task 覆盖成 completed/failed，retry backoff 期间取消也不会再次拉起 child；`SidechainPolicy.timeoutMs` 已接入 `Promise.race` 超时保护，超时会立即 abort child、记录 `EXECUTION_TIMEOUT` 失败且 late success 不会写回 TaskStore；permission guard 新增 `builtin::sidechain-boundary`，会从 hook metadata 中读取 sidechain policy，按 deniedTools、restricted allowedTools 和 workspaceRoot 在工具执行前二次拒绝，避免只依赖 child tool 列表过滤；permission audit entry 会记录 sidechain metadata，`AgentSession`/tool hook executor/`ToolHookExecutor` 类型链路已支持 metadata 透传；`TaskStore` 记录 parentId 并支持 sidechain 更新；测试覆盖 sidechain 参数下发、summary-only 回传、transcript 隔离存储、full_text 显式放开、失败 sidechain metadata、parentId 记录、task_delegate 父 session 与 timeout 透传、真实 sticky child fork 继承父上下文和 permissionMetadata、ephemeral child 完成后清理、executor 运行中 abort 保持 cancelled、retry backoff 取消不重试、sidechain timeout abort 与 late completion 隔离、Orchestrator cancelBackground abort running task、sidechain guard 拒绝 undeclared/denied/workspace escape 以及 audit metadata 回填；`@vitamin/agent`、`@vitamin/hooks`、`@vitamin/tools`、`@vitamin/orchestrator`、`@vitamin/coding` typecheck/build 与目标测试通过。 |
| CCAG-05 | Done |       100% | Pass     | `packages/hooks/src/core/quality/patch-review-gate.ts`、`packages/hooks/src/hook-registry.ts`、`packages/agent/src/session-events.ts`、`packages/coding/src/session/agent-session.ts`、`packages/service/src/inbound-router.ts`、`packages/service/src/session-event-router.ts`、`packages/service/src/ws-protocol.ts`、`packages/hooks/tests/patch-review-gate.test.ts`、`packages/service/tests/inbound-router.test.ts`、`packages/service/tests/session-event-router.test.ts`、`packages/service/tests/websocket-manager.test.ts`                                                                                                                                                                                                                                                                                                       | Patch Review Gate：在写文件、apply patch、运行修改类工具后触发结构化审查门禁，阻止未审查高风险变更直接进入完成态。                   | 已新增 `patch-review-gate` 默认 after hook，接入 `tool.execute.after`，覆盖 `write`、`edit`、`edit-diff`、`apply_patch`、`bash`、`shell` 等修改类工具；工具成功后生成结构化 `patchReview` metadata 和 result details，包含 required、blocked、risk、toolName、targets、reasons；普通文件变更追加 review required 提示，高风险目标（`.env`、`package.json`、lockfile、tsconfig、构建配置、GitHub workflow）和危险 shell 命令（递归删除、git reset/checkout --、chmod 777、curl/wget pipe shell）默认将工具结果标为 error，阻止直接完成；默认 hook preset 已注册该门禁，失败工具结果和只读工具不会重复改写；`AgentSession` 会把 result.details.patchReview 转为 `review_requested`，阻断时同步发 `review_failed` 并记录 pending review，`resolvePatchReview()` 在通过后发 `review_passed` 并解除 pending，拒绝时继续发 `review_failed`；service 支持入站 `Chat.reviewResponse`，路由到目标 session；service 出站为 `Chat.reviewRequested`、`Chat.reviewPassed`、`Chat.reviewFailed`，并复用 WS/SSE/NDJSON session fanout；协议层校验 patch review event schema；测试覆盖普通写入告警、高风险目标阻断、危险 shell 阻断、只读/失败跳过、默认 preset 注册、review response 入站、review requested/failed 路由和 malformed review schema；`@vitamin/hooks`、`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service` typecheck，`@vitamin/hooks`、`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service` build 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| CCAG-06 | Done |       100% | Pass     | `packages/session/src/types.ts`、`packages/session/src/in-memory-session.ts`、`packages/session/src/store.ts`、`packages/session/src/session-manager.ts`、`packages/agent/src/tool-executor.ts`、`packages/coding/src/session/agent-session.ts`、`packages/coding/src/session/coding-session-manager.ts`、`packages/memory/src/file-state-snapshot.ts`、`packages/session/tests/in-memory-session.test.ts`、`packages/session/tests/session-manager.test.ts`、`packages/memory/tests/file-state-snapshot.test.ts`、`packages/agent/tests/tool-executor.test.ts`                                                                                                                                                                                                                                                                            | Session Checkpoint / Rewind：借鉴 checkpoint/rewind 思路，为长任务提供可恢复快照、回退点和元数据水线。                               | 已新增 `SessionCheckpoint`、`createCheckpoint()`、`listCheckpoints()`、`restoreCheckpoint()`，checkpoint 捕获 entries、metadata、leafId、sideEffectCount 和 sideEffects；`toSnapshot()` 会携带 checkpoints 与 sideEffects，`forkSession`、`restoreWithSnapshot`、coding session fork/restore 路径会恢复 checkpoint 和副作用账本；工具 `result` 事件会附带 sideEffects，优先使用工具结果显式声明，否则从 mutating 工具参数启发式提取文件、网络、进程副作用，readonly 工具跳过启发式；`AgentSession` 会把工具副作用写入 session ledger，checkpoint restore 会回退 ledger 到对应水线；`FileStateManager` 支持对 workspace 内 recentFiles 做受控文件内容快照和恢复，记录存在/缺失、文本内容、大小和截断状态，路径越界会跳过；测试覆盖 checkpoint 不可变快照、恢复消息和 metadata、缺失 checkpoint 返回 false、snapshot restore 后仍可 rewind、副作用 ledger 回退、memory/disk persistence 往返、旧 snapshot 兼容、显式/启发式/readonly 工具 sideEffects、文件内容恢复和 workspace 边界；`@vitamin/session`、`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service`、`@vitamin/memory` typecheck，`@vitamin/session`、`@vitamin/agent`、`@vitamin/memory` build，目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| CCAG-07 | Done |       100% | Pass     | `packages/tools/src/plugin-manifest.ts`、`packages/tools/src/types.ts`、`packages/tools/src/tool-registry.ts`、`packages/tools/tests/plugin-manifest.test.ts`、`packages/skill/src/*`、`packages/mcp/src/*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Plugin Manifest：借鉴 Claude Code plugin/skill/MCP 入口，用 manifest 描述工具、命令、技能、MCP server、权限和加载策略。              | 已新增统一 `PluginManifest` schema，覆盖 `id/name/version/status`、tools、skills、mcpServers、permissions；支持权限枚举 `tools/network/filesystem/shell/mcp/skills`，工具级权限声明、deferred tools、preset/category、skill path/trigger、MCP command/url/disabled；新增 `validatePluginManifest()`、`getPluginLoadErrors()`、`summarizePluginManifest()` 和 `buildPluginRuntimePlan()`，可输出 tool registration options、skill refs、MCP server config map、权限、加载 errors/warnings；新增 `discoverPluginManifests()`，从 plugin roots 扫描 `plugin.json`/`vitamin-plugin.json`，输出 discovered manifest、validation、runtimePlan 和 root/read/JSON 错误账本；新增 adapter 驱动的 `applyPluginRuntimePlan()`/`disablePluginRuntimePlan()`，由宿主显式注入 tool/skill/MCP 生命周期适配器，完成工具注册参数下发、skill load/unload、MCP connect/disconnect，并对 disabled、adapter 缺失和执行异常输出结构化 step/errors/warnings，避免 manifest 层直接任意动态执行未知模块；校验覆盖版本/状态、权限枚举、重复 tool/skill/MCP 名称、缺失 loader module warning、无 command/url MCP warning；`ToolMetadata`/`ToolRegistrationOptions` 支持 `pluginId`，工具注册后可追踪来源插件；测试覆盖 manifest 校验、summary、runtime plan、加载错误、manifest discovery、坏 JSON、缺失 root、pluginId metadata、生命周期 apply/disable、adapter 缺失和 adapter 异常；`@vitamin/tools` typecheck/build 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| CCAG-08 | Done |       100% | Pass     | `packages/service/src/websocket-manager.ts`、`packages/service/src/ws-protocol.ts`、`packages/service/src/inbound-router.ts`、`packages/service/src/session-event-router.ts`、`packages/service/src/coding-service.ts`、`packages/service/src/routes/events.ts`、`packages/opendev-ui/src/api/websocket.ts`、`packages/service/tests/websocket-manager.test.ts`、`packages/service/tests/inbound-router.test.ts`、`packages/service/tests/session-event-router.test.ts`、`packages/service/tests/events-route.test.ts`                                                                                                                                                                                                                                                                                                                     | Bridge / Structured Event Reliability：借鉴桥接架构，补齐结构化事件协议、认证、keep-alive、重连和无订阅发送策略。                    | 已新增 `validateWebSocketMessage()`/`isValidWebSocketMessage()`，服务端 `broadcast`、`sendToSession`、`sendToClient` 发送前统一校验协议，未知消息类型、缺少必填字段、非法 `Chat.toolExecutionEvent` 会拒发并记录；`Chat.toolExecutionEvent` 细化校验 tool event 类型、toolCallId/toolName/timestamp；`WebSocketManager` 增加可配置 server heartbeat，周期 ping 客户端并 terminate stale client，同时清理 session subscriptions；服务端支持可选 `websocketAuthToken`，upgrade 阶段校验 `Authorization: Bearer <token>` 或 `?token=`，缺失/错误 token 返回 401 并关闭连接；`InboundRouter` 只允许订阅已存在 session，缺失 session 返回 `Runtime.error`，不加入订阅表；新增 `/api/events/sessions/:id/stream` SSE 和 `/api/events/sessions/:id/ndjson`，二者复用 `WebSocketManager.sendToSession` 的已校验 session fanout；保留 no-subscriber 不广播行为；前端已有 30s ping、指数退避重连和 tab visible reconnect；测试覆盖无订阅不广播、无效出站消息拒发、live/stale heartbeat、upgrade token 校验、session 订阅边界、结构化工具事件 schema、SSE/NDJSON session stream 入口和 session event router；`@vitamin/service` typecheck/build 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| CCAG-09 | Done |       100% | Pass     | `packages/memory/src/memory-extraction.ts`、`packages/memory/src/state-restoration.ts`、`packages/memory/src/semantic-retrieval.ts`、`packages/memory/src/layered-memory.ts`、`packages/coding/src/hooks/memory-extraction.ts`、`packages/memory/tests/semantic-retrieval.test.ts`、`packages/memory/tests/layered-memory.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 分层记忆与状态恢复：借鉴 Claude Code memdir，完善记忆分类、增量提取、相似去重、路径/计划/工具状态恢复和检索质量。                    | 已实现分层记忆、增量提取水线、结构化 tool_use 路径恢复、轻量相似去重；新增 `evaluateSemanticRetrieval()`，可计算 requested、returned、expected、relevant、precision、recall、missing、unexpected，用于检索质量评估；`retrieveRelevantMemories()` 支持 `expectedNames` 质量 trace；Memory frontmatter 支持可选 `scope` 和 `team`，`LayeredMemoryStore`/`InMemoryLayeredStore` 支持 `listScoped()`，并提供 `filterMemoryByScope()`；新增 `detectMemoryConflicts()` 和 `mergeMemoryEntries()`，覆盖 duplicate-name、same-description 冲突检测和合并建议；测试覆盖语义检索质量指标、scope/team 解析与序列化、范围过滤、重复名/同描述冲突和空合并保护；`@vitamin/memory` typecheck/build 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CCAG-10 | Done |       100% | Pass     | `packages/devtools/src/audit-trace.ts`、`packages/devtools/src/devtools.ts`、`packages/devtools/src/tools/debugger.ts`、`packages/devtools/src/service.ts`、`packages/hooks/src/core/permission/audit-log.ts`、`packages/coding/src/session/agent-session.ts`、`packages/coding/src/app/vitamin-app.ts`、`packages/devtools/tests/audit-trace.test.ts`、`packages/devtools/tests/devtools.test.ts`、`packages/devtools/tests/debugger-controller.test.ts`、`packages/devtools/tests/service.test.ts`、`packages/hooks/tests/permission.test.ts`                                                                                                                                                                                                                                                                                            | 安全审计与录制回放：借鉴 VCR/调试能力，为工具调用、模型响应和权限决策提供可复现审计样本。                                            | 已新增 `AuditTraceRecorder` 和 `replayAuditTrace()`，trace schema 包含 version、id、createdAt、metadata、seq/timestamp/type/sessionId/payload，事件类型覆盖 `debug.snapshot`、`tool.execution`、`permission.decision`、`model.response`；默认脱敏 authorization、apiKey/api_key、password、secret、token，支持 maxEvents 环形保留和自定义 clock/metadata；`Debugger.pause()` 自动记录 DebugSnapshot，`AgentSession` 自动记录 tool_execution_event 与 assistant/model response 摘要，`PermissionAuditLog.onRecord()` 让 `VitaminApp` 将权限决策写入 devtools audit trace；`replayAuditTrace()` 校验 trace version、seq 单调、最小事件数、必需事件类型、权限 effect 和工具名，输出 byType、permissionEffects、toolNames 汇总；修复 `Debugger.shouldPause()` 残留硬编码 false，恢复 breakpoint 控制真实生效；补充 `DevtoolsService` 兼容导出和空 SAB 测试占位，避免 devtools 测试被历史兼容问题污染；测试覆盖 trace 录制/脱敏/maxEvents/replay 成功与失败、debug snapshot 自动录制、breakpoint pause 行为、PermissionAuditLog listener、Devtools service start/stop；`@vitamin/devtools`、`@vitamin/hooks`、`@vitamin/coding` typecheck/build 与目标测试通过。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## 阶段完成标准

- 所有 TODO 行状态为 Done，完成百分比为 100%。
- 每个实现项都有输入输出设计、测试设计、实现证据和剩余风险记录。
- `pnpm test -- --run ...` 覆盖本阶段相关测试并通过。
- 相关包级 `typecheck` 通过；若全仓 `pnpm typecheck` 仍失败，必须明确失败是否与本阶段无关。
- 权限、安全、远程事件和文件修改类能力必须有负向测试或明确的定性验证证据。

## 状态更新规则

- `状态` 只能使用 `Not Started`、`In Progress`、`Done`。
- `测试通过` 只能使用 `Pass`、`Fail`、`Not Run`。
- `完成百分比` 使用保守整数；只有实现、测试和设计核对均满足时才能标为 100%。
- 阶段总完成百分比按 TODO 行百分比算术平均并四舍五入计算；当前为 `(100+100+100+100+100+100+100+100+100+100)/10 = 100.0`，四舍五入为 `100%`。本文件头部如更新为不同百分比，必须同步重算。
- 修改任一 TODO 百分比前，必须先读取对应实现和测试，不得只基于计划调整。

## RFC-to-TODO 执行记录

- 2026-05-02：读取 `docs/rfc/claude-code-source-analysis.md`，将 14 个 Claude Code 借鉴项合并为 10 个执行任务。
- 2026-05-02：核对当前实现证据：prompt cache、deferred tools、tool executor、memory extraction、state restoration、auto compaction、websocket、session metadata。
- 2026-05-02：执行目标测试命令，结果为 10 个测试文件通过，123 个测试用例通过。
- 2026-05-02：推进 `CCAG-01`，新增不可绕过安全策略，补充 bypass 下 protected path/root removal 负向测试；更新目标测试结果为 11 个测试文件通过，173 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-01`，新增 `PermissionContext.filePaths` 多路径规则匹配、guard 多参数路径抽取、directory-freeze 全路径约束；更新目标测试结果为 11 个测试文件通过，176 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-01`，限制配置型 permission policy 最小优先级为 25，防止用户配置负优先级绕过 builtin safety；更新目标测试结果为 11 个测试文件通过，178 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-01`，新增 `PermissionContext.urls`、guard URL 参数抽取和 `builtin::network-safety`，覆盖 web/MCP 网络工具的 localhost/private/metadata/非 HTTP(S) 拒绝；更新目标测试结果为 11 个测试文件通过，184 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-01`，在 MCP client 连接前校验 SSE server URL，拒绝 localhost/private/metadata/非法协议；修正 MCP client 测试导入到迁移后的 `packages/mcp/src`；更新目标测试结果为 12 个测试文件通过，202 个测试用例通过。
- 2026-05-02：完成 `CCAG-01`，tool executor 对 `[CONFIRM]` ask 决策改为 fail-closed，缺少 approval handler 时拒绝执行，approve/reject/no-handler 路径均有测试；更新目标测试结果为 12 个测试文件通过，205 个测试用例通过。
- 2026-05-02：推进 `CCAG-02`，新增 context budget planner 纯函数和 `MemoryManager.planContextBudget()`，补充 none/prune/compact/snip 与 manager 暴露测试；更新目标测试结果为 13 个测试文件通过，210 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-02`，auto-compaction hook 接入 `planContextBudget()`，模型调用前低水位跳过压缩处理，高水位再进入 process，并回填 `contextBudget` metadata；更新目标测试结果为 13 个测试文件通过，211 个测试用例通过。
- 2026-05-02：完成 `CCAG-02`，auto-compaction hook 在 process 后回填 before/after 预算、策略列表、tokensBefore/tokensAfter/tokensSaved 和 changed 状态，形成压缩前后 token 账本；更新目标测试结果为 13 个测试文件通过，211 个测试用例通过。
- 2026-05-02：推进 `CCAG-03`，新增 `ToolExecutionEvent` 与 `executeStream()`，覆盖工具 started、approval、error、result 结构化事件；补充 4 条事件流测试；更新目标测试结果为 13 个测试文件通过，215 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-03`，新增 progress 事件并将 tool `onUpdate` 接入 `executeStream()`；work-loop 改为消费事件流并发出 `tool_execution_event`，同时保留旧 `tool_call_start/end`；补充 agent-loop progress 透传测试；更新目标测试结果为 14 个测试文件通过，224 个测试用例通过。
- 2026-05-02：完成 `CCAG-03`，session/service/UI 桥接 `tool_execution_event` 为 `Chat.toolExecutionEvent`，前端将 progress 附加到对应工具消息；补充 service router 测试；更新目标测试结果为 15 个测试文件通过，225 个测试用例通过。
- 2026-05-02：推进 `CCAG-06`，新增 session checkpoint/rewind API、checkpoint snapshot 持久化、fork/restore 传递 checkpoint 账本和 in-memory session 回退测试；`@vitamin/session` typecheck/build 通过，目标测试结果更新为 15 个测试文件通过，229 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-06`，新增 session side-effect ledger、checkpoint sideEffects 水线、工具 result sideEffects 提取和 `AgentSession` ledger 记录；补充副作用回退、显式副作用、启发式副作用和 readonly 跳过测试；`@vitamin/session`、`@vitamin/agent`、`@vitamin/coding` typecheck 通过，`@vitamin/session` 与 `@vitamin/agent` build 通过，目标测试结果更新为 15 个测试文件通过，233 个测试用例通过。
- 2026-05-02：完成 `CCAG-06`，补齐 memory/disk persistence checkpoint 与 sideEffect 往返、旧 snapshot 兼容测试，并在 `FileStateManager` 增加 workspace 内 recentFiles 文件内容快照/恢复能力和越界保护；`@vitamin/memory` typecheck/build 通过，扩展目标测试结果更新为 17 个测试文件通过，258 个测试用例通过。
- 2026-05-02：推进 `CCAG-08`，新增服务端 WebSocket 出站协议校验、无效消息拒发、可配置 server heartbeat 和 stale client 清理；补充 schema、heartbeat 和拒发测试；`@vitamin/service` typecheck/build 通过，扩展目标测试结果更新为 17 个测试文件通过，262 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-08`，新增可选 `websocketAuthToken` upgrade 校验和 session subscribe 存在性边界；补充 token 拒绝/放行与非法 session 订阅测试；`@vitamin/service` typecheck/build 通过，扩展目标测试结果更新为 18 个测试文件通过，265 个测试用例通过。
- 2026-05-02：完成 `CCAG-08`，新增 session event SSE/NDJSON 兼容入口，复用已校验 `sendToSession` fanout；细化 `Chat.toolExecutionEvent` schema 校验；补充 event sink、SSE/NDJSON route 和 malformed nested event 测试；`@vitamin/service` typecheck/build 通过，扩展目标测试结果更新为 19 个测试文件通过，269 个测试用例通过。
- 2026-05-02：推进 `CCAG-05`，新增默认 `patch-review-gate` after hook，修改类工具成功后生成结构化 review metadata，普通变更提示审查，高风险目标和危险 shell 命令将结果标记为 error；补充 patch-review gate 测试；`@vitamin/hooks` typecheck/build 通过，扩展目标测试结果更新为 20 个测试文件通过，274 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-05`，将 patchReview 结果转为 session 级 `review_requested`/`review_failed` 事件，service 映射为 `Chat.reviewRequested`/`Chat.reviewFailed` 并补充 review schema 校验；`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service` typecheck 通过，`@vitamin/agent`、`@vitamin/service` build 通过，扩展目标测试结果更新为 20 个测试文件通过，276 个测试用例通过。
- 2026-05-02：完成 `CCAG-05`，新增 pending patch review 账本、`resolvePatchReview()`、入站 `Chat.reviewResponse` 和 `review_passed` 事件；review 通过可解除 pending 审计阻断，拒绝继续发 failed；`@vitamin/coding`、`@vitamin/service` typecheck 通过，`@vitamin/agent`、`@vitamin/coding`、`@vitamin/service` build 通过，扩展目标测试结果更新为 20 个测试文件通过，277 个测试用例通过。
- 2026-05-02：完成 `CCAG-09`，新增语义检索质量评估、memory scope/team frontmatter 与范围过滤、memory 冲突检测和合并建议；`@vitamin/memory` typecheck/build 通过，扩展目标测试结果更新为 22 个测试文件通过，309 个测试用例通过。
- 2026-05-02：推进 `CCAG-07`，新增 plugin manifest schema、校验、summary、runtime plan 和 tool registry `pluginId` metadata；补充 manifest 校验、冲突、加载错误、runtime plan 与 pluginId 测试；`@vitamin/tools` typecheck/build 通过，扩展目标测试结果更新为 23 个测试文件通过，314 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-07`，新增 plugin manifest 文件 discovery，扫描 `plugin.json`/`vitamin-plugin.json` 并输出 validation/runtimePlan/load errors；补充有效 manifest、坏 JSON、缺失 root 测试；`@vitamin/tools` typecheck/build 通过，扩展目标测试结果更新为 23 个测试文件通过，315 个测试用例通过。
- 2026-05-02：完成 `CCAG-07`，新增 `applyPluginRuntimePlan()`/`disablePluginRuntimePlan()` 生命周期执行层，通过宿主注入 adapter 对接工具注册、Skill 加载和 MCP 启停，并输出结构化 step/errors/warnings；补充 apply、disable、adapter 缺失和异常测试；`@vitamin/tools` typecheck/build 通过，扩展目标测试结果更新为 23 个测试文件通过，318 个测试用例通过。
- 2026-05-02：推进 `CCAG-04`，新增 orchestrator sidechain 执行契约、summary-only 默认回传、child transcript 隔离存储和 parentId 追踪；补充参数下发、summary-only、full_text、transcript 隔离和 parentId 测试；`@vitamin/orchestrator` typecheck/build 通过，扩展目标测试结果更新为 26 个测试文件通过，366 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-04`，`task_delegate` 从工具上下文透传 parentSessionId，`VitaminApp.runSession` 对 sidechain 从父 session fork child session，支持 sticky 保留、ephemeral 清理、summary/transcript 返回、restricted tool 过滤和 workspaceRoot 覆盖；补充 task_delegate 父 session 透传和真实 sticky child fork 继承父上下文测试；`@vitamin/tools`、`@vitamin/orchestrator`、`@vitamin/coding` typecheck/build 通过，扩展目标测试结果更新为 28 个测试文件通过，381 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-04`，sidechain 执行失败时回填 childSessionId、失败摘要和 transcript metadata，`VitaminApp.runSession` 在 sidechain 成功/失败路径均通过 finally 清理 ephemeral child session；补充失败 metadata 和 ephemeral child 清理测试；`@vitamin/orchestrator`、`@vitamin/coding` build 通过，扩展目标测试结果更新为 28 个测试文件通过，383 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-04`，新增 `RunSessionOptions.signal`、TaskExecutor active AbortController、Orchestrator cancelBackground/updateTask cancel abort 链路和 AgentSession prompt 外部 signal 接入；运行中取消会 abort child agent，runSession 晚返回不会覆盖 cancelled 状态，retry backoff 期间取消也不会再次拉起 child；补充 executor 运行中 abort、retry backoff 取消不重试和 Orchestrator cancelBackground abort running task 测试；`@vitamin/orchestrator`、`@vitamin/coding` typecheck/build 通过，扩展目标测试结果更新为 28 个测试文件通过，386 个测试用例通过。
- 2026-05-02：继续推进 `CCAG-04`，新增 `SidechainPolicy.timeoutMs`、`task_delegate.timeoutMs` 透传和 TaskExecutor `Promise.race` 超时保护；超时会 abort child、记录 `EXECUTION_TIMEOUT`，并隔离 late completion 写回；补充 task_delegate timeout 透传和 sidechain timeout abort/late completion 测试；`@vitamin/tools`、`@vitamin/orchestrator`、`@vitamin/coding` typecheck/build 通过，扩展目标测试结果更新为 28 个测试文件通过，387 个测试用例通过。
- 2026-05-02：完成 `CCAG-04`，新增 permission hook metadata 链路、`builtin::sidechain-boundary`、sidechain policy audit metadata 记录和 coding child session permissionMetadata 透传；补充 sidechain undeclared/denied/workspaceRoot guard、audit metadata、真实 sticky child permissionMetadata 测试；`@vitamin/agent`、`@vitamin/hooks`、`@vitamin/tools`、`@vitamin/orchestrator`、`@vitamin/coding` typecheck/build 通过；目标测试 `packages/hooks/tests/permission.test.ts packages/coding/tests/vitamin-app-slot-routing.test.ts packages/orchestrator/tests/executor.test.ts packages/orchestrator/tests/orchestrator.test.ts packages/tools/tests/extended-tools.test.ts` 结果为 5 个测试文件、123 个测试用例通过；`pnpm test` 全仓仍受既有 dist/export、沙箱写入和网络 DNS 用例影响为 Partial。
- 2026-05-02：完成 `CCAG-10`，新增 devtools audit trace recorder/replayer、默认脱敏、trace summary/replay 断言、debug snapshot 自动录制、tool execution/model response 自动录制和 permission audit listener 接入；修复 Debugger breakpoint pause 硬编码 false、补齐 `DevtoolsService` 兼容导出和空 SAB 测试占位；`@vitamin/devtools`、`@vitamin/hooks`、`@vitamin/coding` typecheck/build 通过；目标测试 `packages/devtools/tests packages/hooks/tests/permission.test.ts` 结果为 7 个测试文件、88 个测试用例通过，其中 devtools service 测试需非沙箱本地端口监听权限。
- 2026-05-02：记录全仓 `pnpm typecheck` 剩余失败点：`packages/invariant`、`packages/setting`、`packages/swarm`，暂未归入本阶段直接缺口。
- 2026-05-02：收尾质量推进，修复 `packages/invariant` console 参数类型、补齐 `tsup-strip-invariant-plugin` 兼容入口，修复 `packages/setting` 的 `WORKFLOW_SLOTS` 导出和 optional `disabled_tools` 测试类型，修复 `packages/swarm` Agent 构造配置和 TS 6 deprecation 设置；`pnpm typecheck` 全仓 21 个项目通过；目标测试 `packages/setting/tests packages/swarm/tests packages/invariant/tests` 结果为 12 个测试文件、95 个测试用例通过。
- 2026-05-02：继续收尾全仓测试，修复 HookRegistry legacy hook 兼容、Agent 无参构造/`aborted` 事件/run-context override、CodingSessionManager legacy default merge、AgentSession 旧构造签名、agent factory、MCP tools 迁移 re-export、MCP stdio spawn error 快速失败和空测试占位；`pnpm typecheck` 全仓 21 个项目通过；目标兼容测试结果为 5 个测试文件通过、2 个 skipped，54 个测试用例通过、2 个 skipped；`pnpm test` 沙箱内剩余 3 个测试文件、8 个用例失败，均为 localhost listen、`~/.vitamin` 写入、外网 DNS 环境类；非沙箱复核这 3 个测试文件 52 个用例全部通过。
- 2026-05-02：完成全仓测试收尾，`web_search` 常见查询测试改为 mock `fetch` 返回固定 Brave 风格 HTML，避免全仓测试依赖第三方搜索服务实时可用性和页面结构变化；`packages/tools/tests/web-tools.test.ts` 37 个测试用例通过；非沙箱 `pnpm test` 全仓 142 个测试文件通过、2 个 skipped，1358 个测试用例通过、2 个 skipped；`pnpm typecheck` 全仓 21 个项目通过。
- 2026-05-02：完成质量门禁收尾，将 `@vitamin/swarm` build 脚本从缺失的 `tsup` 统一为仓库现用 `tsdown`，修复新增代码中的 20 个 oxlint error，并执行 `oxfmt` 机械格式化；`pnpm lint`、`pnpm typecheck`、非沙箱 `pnpm test`、`pnpm build` 全部通过，其中测试结果为 142 个测试文件通过、2 个 skipped，1358 个测试用例通过、2 个 skipped，构建结果为 22 个项目通过。
- 2026-05-02：执行发布前总门禁 `pnpm prepublish:check`，串联验证 `pnpm lint && pnpm typecheck && pnpm test && pnpm build` 全部通过；当时可发布门禁剩余风险仅为 oxlint warning（89 个，不阻断），无 lint error、类型错误、测试失败或构建失败，后续已清零。
- 2026-05-02：继续质量门禁清理，消除剩余 89 个 oxlint warning，重点移除非空断言、补齐数组/Map/JSON-RPC id/sidechain sticky session 的显式收窄，并将简单字符串拼接统一为模板字符串；重新执行 `pnpm lint`、`pnpm typecheck` 和 `pnpm prepublish:check` 全部通过，当前 lint 状态为 oxlint 0 warning、0 error。
